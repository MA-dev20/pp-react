<div id="game_channel" data-game-id='<%= @game.id %>'></div>
<div class="head">
	<h1>Du hast für <%= @turn.user.fname %> gestimmt!</h1>
</div>
<div class="middle">
	<% if @turn.user.avatar? %>
	<%= image_tag @turn.user.avatar.url, class: 'custom-border pulse' %>
	<% else %>
	<div class="circle"><%= @turn.user.fname[0].capitalize %><%= @turn.user.lname[0].capitalize %></div>
	<% end %>
</div>
<div class="bottom">
	<% if @admin == @game.user %>
	<%= link_to 'weiter', gm_set_state_path('', state: 'turn'), class: 'btn custom-btn' %>
	<% end %>
</div>
