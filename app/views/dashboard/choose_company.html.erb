<% @companies.each do |c| %>
  <% if c.company_users.find_by(user: @admin).role != 'inactive' || c.company_users.find_by(user: @admin).role != 'inactive_user' %>
    <%= link_to c.name, login_company_path(c.id) %>
  <% end %>
<% end %>
